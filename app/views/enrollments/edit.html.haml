- content_for :title do
  Corsego
  \|
  Add review
  = @enrollment.to_s

.card
  .card-header
    %h3
      .text-warning.fa.fa-star
      Add a Review
  .card-body
    = render 'form'
  .card-footer
    = link_to 'Show Review', @enrollment
    \|
    = link_to "Back to #{@enrollment.course} course", course_path(@enrollment.course)
