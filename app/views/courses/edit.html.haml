- content_for :title do
  Corsego
  \|
  Edit
  = @course.title
.card
  .card.card-header
    %h3
      .fa.fa-graduation-cap
      Editing course
  .card.card-body
    = render 'form'

= link_to 'Show', @course
\|
= link_to 'Back', courses_path
